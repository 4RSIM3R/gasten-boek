import { AppLayout } from "@/layouts/app-layout";

export default function Analytic() {
    return (
        <div>
            analytic
        </div>
    )
}

Analytic.layout = (page: any) => <AppLayout children={page} />;